<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_dms_pdf_merge_form" model="ir.ui.view">
        <field name="name">dms.pdf.merge.form</field>
        <field name="model">dms.pdf.merge</field>
        <field name="arch" type="xml">
            <form string="Merge PDFs">
                <group>
                    <field name="new_filename"/>
                    <field name="file_ids" widget="many2many_tags" readonly="1"/>
                </group>
                <footer>
                    <button string="Merge" name="action_merge" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_dms_pdf_merge" model="ir.actions.act_window">
        <field name="name">Merge PDFs</field>
        <field name="res_model">dms.pdf.merge</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="dms.model_dms_file"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
